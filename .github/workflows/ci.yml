#name: CI
#
#on:
#  push:
#    branches: [ main ]
#    tags: [ 'v*' ]
#  pull_request:
#    branches: [ main ]
#
#env:
#  CARGO_TERM_COLOR: always
#
#jobs:
#   docker:
#     runs-on: ubuntu-24.04-arm
#     
#     steps:
#     - uses: actions/checkout@v4
#     
#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3
#     
#     - name: Build Docker image for arm64       
#       run: docker buildx build --platform linux/arm64 -t bridge-cli:arm64 -f ./dockerfiles/bridge-cli.dockerfile .                                                                                   
#     - name: Save Docker image
#       run: docker save -o bridge-cli-docker-image.tar bridge-cli:arm64
#     
#     - name: Upload Docker image artifact
#       uses: actions/upload-artifact@v4
#       with:
#         name: bridge-cli-docker-image
#         path: bridge-cli-docker-image.tar
